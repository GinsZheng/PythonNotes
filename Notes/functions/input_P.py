#input
age = input('How old are you? \n>')
print("So, your're %r old" %age)

#🌁二确确认
input('Ready, hit RETURN to continue, CTRL-C to abort.')

#Python3的raw_input似乎不可用,raw_input时转义失效(为纯文本)